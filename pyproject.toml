[project]
name = "agentdock"
version = "0.13.0"
description = "Local supervised multi-agent control plane for Codex"
requires-python = ">=3.11"
dependencies = []

[tool.agentdock]
start-command = "python3 agentdock.py"
test-command = "python3 -m unittest discover -s tests -v"
